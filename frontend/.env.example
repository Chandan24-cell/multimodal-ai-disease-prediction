# Local Docker development. The browser reaches the published backend port.
VITE_API_URL=http://localhost:8000

# For a separate Vercel deployment, use:
# VITE_API_URL=https://your-backend.onrender.com/api